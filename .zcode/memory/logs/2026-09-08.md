# 2026-09-08

## 完成的工作（晚3：D11-14 演出闭环 + Android/CI）

- **D11-12 演出**：scripts/view/battle_playback.gd（BattlePlayback：节奏/1x-2x/跳过/finished 恰一次）+ battle_view.gd + scenes/battle.tscn（主场景已设 run/main_scene）；ConfigService 补 get_all()；关卡下拉（StageConfig 首波）；重播=同种子复现。主场景 headless --quit-after 120 跑通全链
- **D13-14**：export_presets.cfg（Android APK：arm64-v8a、include_filter="*.json"、minSdk 26 写在 gradle_build 段、包名 com.ligoudan95.fangzhi）；竖屏靠项目设置 handheld/orientation。CI：lint/test 去掉 continue-on-error 转强制；新增 android-apk job（模板缓存 + keytool 生成 debug keystore + 写 editor_settings-4.7.tres 注入 SDK/keystore + export-debug + artifact）
- 本机无 Java/Android SDK（编辑器设置里的 SDK/keystore 路径是从旧机带来的空配置）——真机验证与本地出包待用户补环境；CI runner 出包不受影响
- 测试 28/28（+6 演出：回放器 4 + 场景冒烟 2）；check_all 七步全绿
- 新坑记录：gdUnit 6.2.1 无 add_child_autofree（用 auto_free+add_child）；断言是 .contains() 不是 .is_contains()；lambda 按值捕获整型（计数用字典）；dep_check 连注释里的类名都查（config 层注释不能提 BattleSetup）
- **docs/13 §13 D1-14 自动化部分全部完成**；剩：真机验证（人工）+ Phase 0 余项

## 完成的工作（晚2：godot_ai 升级 4.0.2）

- addons/godot_ai 4.0.0 → **4.0.2**（官方 release 资产 `godot-ai-v4-plugin.zip`，走 7897 代理下载；注意 `godot-ai-plugin.zip` 是 v3→v4 迁移桥不是完整插件）
- `.zcode/config.json`：pip 包 3.2.4 → 4.0.2；**4.x 删除了 `attach` 子命令**（CLI 平铺，默认 stdio 传输，--ws-port 9500 连插件）
- godot-ai-LICENSE.txt 同步更新；autoload 路径不变（runtime/game_helper.gd）
- 验证：--import 类注册正常（新增 McpAtomicWrite/McpClient 等类），check_all 七步全绿
- feitu 仍是 3.2.4 + 4.0.0，未动（用户仅要求 fangzhi）
- 4.x 插件 README：插件自启 MCP server 并经 WebSocket 连接，编辑器内无需手动配置

## 完成的工作（晚：D6-10 战斗引擎移植 + 对拍上线）

- **GDScript 引擎移植**（与 src/battle/engine.ts 逐行对齐）：
  - rng.gd：BattleRng mulberry32——32 位运算全掩码，乘法 _mul32 拆 16 位防 int64 溢出；含 TS 黄金值单测（42/1/20260908/70000 × 5 值）
  - engine.gd：Battle 类——clash/calc_damage/compute_capture_rate 纯函数 + 回合流程（先攻排序/控制跳过/AI 决策/技能效果九类/DOT/替补/冰冻转灼烧/催眠受击解除/捕捉）
  - battle_setup.gd：BattleSetup——load_tables/build_cfg/make_pet_input/group_inputs（技能池槽位 0 基）
- **对拍设施**：tools/parity/seeds.json（100 种子：20260908+i×7，入 Git）；npm run parity = 导表+回填+生成期望日志；tests/parity_runner.gd（SceneTree，逐行 diff，首个差异行打印 GD/TS 对照）
- **验证**：PARITY OK 一次通过（100 种子 × 3 场景）；GdUnit4 22/22；check_all 升为七步全绿（7/7 对拍）；CI 新增 parity 阻塞 job
- 修复记录：RNG 消耗顺序（pickByStrategy 惰性化）、uid 未自增、cds.resize 填 null、gdlint（max-returns/参数名/未用参数）、Variant 推断错误 ×2
- 关键结论：GDScript 与 TS 的对拍可行且已闭环——此后任何一侧公式改动，check_all 第 7 步立即报警

## 完成的工作（下午：Godot D1-2 + D3-5）

- **D1-2 工程骨架**：scripts/{infra,config,battle,logic,view} + scenes/；Autoload 四件套注册（Event/Config/Save/Audio + _mcp_game_helper）；default_bus_layout.tres（BGM/Amb/SFX_Battle/SFX_Work/UI 五总线）；tools/sync_config.ts + `npm run sync:godot`（导表→回填 resources/config/，11 张表）
- **D3-5 数据与成长层**：ConfigService.gd（load_all 幂等/get_table→Variant/get_rows/get_g）；stats.gd（BattleStats 静态三函数，与 src/battle/stats.ts 逐行对齐）；tests/ 新增 GdUnit4 8 项（突破倍率分段、V-401 战力锚点、TS 黄金值全等、res 随 mag 资质、ConfigService×4）
- **check_all: ALL OK**——TS 导表+15 测试 / dep_check(5 .gd) / gdformat / gdlint / GdUnit4 8/8
- 踩坑记录：`:=` Variant 推断=4.7 错误级；int/int 整除破坏对拍（须 /1000.0）；GdUnit -s 需先 `--import` 建类缓存；4.7 拒绝 remote-debug 端口 0（check_all 已修）

## 完成的工作（上午：仓库初始化 + feitu 设施移植）

### 仓库初始化
- 从 `github.com/ligoudan95/fangzhi.git` 克隆（直连超时，走本机 7897 代理）
- TS 基线验证全绿：`npm run export`（11 表 / 251 行 / V-101~501 校验全过）、`npm test` 15/15

### Godot 骨架修正（D1-2 前置）
- project.godot：工程名 fnagzhi→fangzhi；渲染 mobile→gl_compatibility（desktop+mobile），删 d3d12；补 1080×1920 + portrait
- .gitignore：Godot 模板与原 TS 忽略（out/ node_modules/ *.log .DS_Store）合并
- tables/ 加 .gdignore，删除 96 个 .translation/.csv.import 垃圾；.godot/ 缓存清理

### 从 feitu 移植开发设施
- addons/gdUnit4 6.2.1（docs/13 §12 点名的测试框架）、addons/godot_ai 4.0.0（MCP 驱动编辑器）+ godot-ai-LICENSE.txt
- project.godot 启用两插件 + `_mcp_game_helper` autoload
- .zcode/：config.json（MCP）、agents/ 五角色（planner/architect/programmer/docs-updater/git-admin，按 fangzhi 铁律重写）、commands/team-lead.md、hooks/（ask_gate + ask_timeout_gate 原样）、memory/（MEMORY.md + 本日志）
- .github/workflows/ci.yml：ts（导表+测试）+ lint（gdtoolkit+dep_check）+ test（GdUnit4）三 job
- tools/dep_check.ps1（分层依赖方向检查，适配 scripts/ 五层）、tools/check_all.ps1（一键检查，加 TS 两步）
- AGENTS.md 起草（铁律/命名/命令/测试策略/当前状态）

## 修改的文件
- project.godot、.gitignore、AGENTS.md（新建）
- addons/（新增 6.6MB）、.zcode/（新增）、.github/（新增）、tools/dep_check.ps1、tools/check_all.ps1（新增）

## 核心要点
- 数值链路唯一合法路径：tables/*.csv → npm run export → out/config/*.json → 构建拷贝 → resources/config/；引擎不许直接读表
- TS 引擎（src/battle）是数值验收基准；Godot 版移植后同种子对拍，连续 4 周全绿前不退役 TS 侧
- hooks 在用户级 config.json 全局注册（当前指向 feitu 路径，内容与项目无关，全局生效）
