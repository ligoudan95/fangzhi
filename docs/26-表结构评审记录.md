# 26-表结构评审记录（M4 村落生产 / M6 FTUE）

> **版本**：v0.1　**日期**：2026-09-12
> **性质**：程序评审记录（AGENTS 铁律 6：表结构变更走程序评审）；评审对象为村落生产闭环与 FTUE 主线数据所需的表结构增量
> **结论**：**通过，分期落地**——Phase A（本记录 §2.1/§2.2/§3）先行，Building/Recipe 完整表与 FTUE 教学字段随后

---

## 1. 评审范围与原则

1. 增量不破坏既有 14 表导出与对拍；新表 PK 即刻注册，业务校验随逻辑落地逐步收紧。
2. 所有引用走 `ref<T>`；枚举只尾部追加。
3. Phase 0 只落当前阶段实际消费的字段；为 Phase 1+ 预留但不实现行为的字段不加（避免死字段）。

## 2. Phase A：作物生产闭环（本批）

### 2.1 新表 `ItemBase.csv`（物品主档）

| 行 | 值 |
|---|---|
| 字段 | `itemId,name,kind,storageCategory` |
| 类型 | `int,string,enum<ItemKind>,ref<StorageRule>` |
| 可见性 | `c,c,c,c` |
| 注释 | `主键,物品名,物品类别,仓储分类（容量归属）` |

Phase A 落地作物产物 4 行 + 兽贝/灵晶不入 ItemBase（货币走 wallet，docs/15 §3.2）。`ItemKind` 枚举仅 `1 材料` 一档起步。

### 2.2 新表 `StorageRule.csv`（仓储容量）

| 行 | 值 |
|---|---|
| 字段 | `categoryId,baseCap` |
| 类型 | `int,int` |
| 可见性 | `c,c` |
| 注释 | `主键（1矿石2草药3食物4金属锭5建材）,基础容量（软上限）` |

docs/05 §11 基准：200/150/100/100/150。软容量语义：可超上限保留，每满 24h 衰减 10%（docs/15 §2.4）；仓库等级扩容公式 `base×1.5^(level-1)` 属 Building 联动，Phase A 仅落基础容量。

### 2.3 新表 `SeasonWeather.csv`（四季系数）

| 行 | 值 |
|---|---|
| 字段 | `seasonId,name,farmMult,mineMult,beastMult` |
| 类型 | `int,string,float,float,float` |
| 可见性 | `c,c,c,c,c` |
| 注释 | `主键（0春1夏2秋3冬）,季节名,灵田产量系数,矿场系数,凶兽潮系数` |

docs/05 §8：春 ×1.2 / 夏 ×1.0 / 秋 ×1.3 / 冬 ×0.5（矿场另值）。天气随机事件不入 Phase A（docs/15 §2.6）。

### 2.4 `Crop.csv` 扩展

新增列 `outputItemId:ref<ItemBase>,outputCount:int`（产物与数量）；补齐 docs/05 §5 全 7 行（玄冰苔、千年桃木）。既有 4 行回填产物指向。

### 2.5 `GlobalConst.csv` 新键

| 键 | 值 | 依据 |
|---|---|---|
| `SEASON_EPOCH_UTC_SEC` | 1735689600（2025-01-01T00:00Z） | 全局季纪元（docs/15 §1.3；所有档同季） |
| `OFFLINE_CAP_BASE_SEC` | 43200 | 12h 基础上限（docs/15 §2.2） |
| `OFFLINE_CAP_TOTEM_SEC` | 86400 | 图腾二段 24h |
| `STORAGE_DECAY_PCT` | 0.1 | 溢出 24h 衰减 10% |
| `STORAGE_DECAY_PERIOD_SEC` | 86400 | 衰减周期 |

### 2.6 `Enums.csv` 追加

`ItemKind,1,材料`（仅此一档；后续丹药/图纸等尾部追加）。

## 3. Phase A 逻辑与测试（伴随落地）

- `scripts/logic/inventory_ledger.gd` + `src/logic/inventoryLedger.ts`：`add_item` 软容量入库（返回新增/超量）；`apply_overflow_decay` 按 24h 边界确定性衰减（比例分摊、余数按 itemId 升序）。
- `scripts/logic/village_production.gd` + `src/logic/villageProduction.ts`：`settle(state, now, config)`——按 `UtcTimeSlicer.slice_range` 分段套季节系数；成熟田块产出物品进库存；游标推进；纯函数返回 `{next_state, report}`，不原地改输入（docs/15 §3.5）。
- 测试：黄金值/跨季分段/软容量/衰减确定性/幂等（同输入同输出）；parity 追加 `[farm]` 区段。

## 4. Phase B（本节为评审结论，随后续实现落地）

### 4.1 `Building.csv`（建筑全表，docs/05 §2）

| 行 | 值 |
|---|---|
| 字段 | `buildingId,name,maxLevel,unlockRealm,effectKind,effectBase,effectStep,upgradeCostKey,upgradeCostBase,upgradeCostStep` |
| 类型 | `int,string,int,string,enum<BuildingEffect>,float,float,float,string,int,int` |
| 可见性 | `c,c,c,c,c,c,c,c,c,c` |

- `BuildingEffect` 枚举（Enums 尾加）：`1 上限解锁 2 数值加成 3 队列容量 4 恢复速率 5 城防`。
- 15 建筑逐行落表（议事堂/民居/兽栏/灵田/矿场/城墙箭塔/锻造炉/药庐/仓库/集市/演武场/图腾柱/祭坛/藏宝洞）。
- 升级消耗走 `Recipe` 引用或独立成本字段——倾向独立字段（升级不是配方）。

### 4.2 `Recipe.csv`（锻造/药庐生产链，docs/05 §7）

| 行 | 值 |
|---|---|
| 字段 | `recipeId,station,inputs,outputItemId,outputCount,durationMin,unlockRealm` |
| 类型 | `int,enum<Station>,string,ref<ItemBase>,int,int,string` |
| 可见性 | `c,c,c,c,c,c,c` |

- `inputs` 格式 `itemId:count;itemId:count`（定长不定，新增复合类型 `pairs<ItemBase>` 走工具改造）。
- `Station`：`1 锻造炉 2 药庐`。

### 4.3 `Mine.csv`（矿层，docs/05 §6）

| 行 | 值 |
|---|---|
| 字段 | `mineId,unlockRealm,ironRate,crystalRate,refinedRate,collapseRisk` |
| 类型 | `int,string,int,int,int,float` |

### 4.4 派遣体力/心情（不建新表）

`village.petConditions` 已预留（docs/15 §3.2）；参数走 `GlobalConst`（`STAMINA_MAX`/`STAMINA_DRAIN_H`/`MOOD_MAX` 等），公式进 `docs/02 §6.2` 引用。

### 4.5 兽潮错过结算（docs/15 §1.5/§2）

- 波次边界：本地 12:00/20:00 → UTC（`tz_offset_sec` 由 View 层注入，logic 只收整数秒）。
- `BeastTide.settle_missed(state, cursor, now, tz_offset, config)`：枚举 [cursor,now) 内波次 → 逐波 `Battle`（守村队）→ 胜负落 `DropRule×2.0`（docs/05 §9）→ 报告 ID `beast_tide:<wave_utc>`。
- 表无结构变更；`BeastTide.csv`（波次模板）Phase B 首批随实现落表。

### 4.6 影响面

`PRIMARY_KEYS` +3 表；`Enums` +2 组（BuildingEffect/Station）；`GlobalConst` +8~10 键；对拍追加 `[build]/[recipe]/[beast]` 区段；`ConfigTypes`/`fields.md` 自动再生成。

## 5. 影响面清单

| 项 | 变更 |
|---|---|
| `tools/export/export.ts` | PK 注册 3 表；Crop 列扩；ItemBase 的 `kind` 枚举、`storageCategory` 外键自动校验；GlobalConst 新键随行 |
| `ConfigTypes`/`docs/fields.md` | 自动再生成（漂移校验兜底） |
| 对拍 | 期望日志新增 `[farm]` 区段 |
| 存档 | `village.fields/storage` 形状已由 docs/15 §3.2 预留，无 schema 变更 |

## 6. 评审结论

通过。风险：仓储衰减的整数分摊需双端一致（TS/GD 取整次序差异是历史雷区）；已在测试矩阵中以黄金值锁定。
